// Udinese official store (store.udinese.it) — currently offline / under maintenance.
// Returns an empty array until the store is back online.
import { Product } from '../../shared/Product';

const scrapeUdinese = async (): Promise<Product[]> => [];

export default scrapeUdinese;
