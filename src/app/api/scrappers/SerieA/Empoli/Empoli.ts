// shop.empolifc.it — fully JS-rendered Wix/webrepository platform.
// Server-side fetch returns 403; no product data available in static HTML.

import { Product } from '../../shared/Product';

const scrapeEmpoli = async (): Promise<Product[]> => [];

export default scrapeEmpoli;
