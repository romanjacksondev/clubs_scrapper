// Hull City — no publicly accessible online store found.
// All routes under hullcityafc.net/shop redirect back to the club homepage.
import { Product } from '../../shared/Product';

const scrapeHullCity = async (): Promise<Product[]> => {
  return [];
};

export default scrapeHullCity;
