// Charlton Athletic official store (clubshop.cafc.co.uk) — Cloudflare bot protection.
// All programmatic requests are blocked with HTTP 403 (Cloudflare).
// A browser-automation solution would be needed to bypass this.
import { Product } from '../../shared/Product';

const scrapeCharltonAthletic = async (): Promise<Product[]> => {
  return [];
};

export default scrapeCharltonAthletic;
