// Portsmouth FC official store (pompey.clubstore.co.uk) — nopCommerce / custom SPA.
// All product pages are dynamically rendered via client-side JavaScript.
// Server-side HTML contains no product data; a browser-automation solution is needed.
import { Product } from '../../shared/Product';

const scrapePortsmouthFC = async (): Promise<Product[]> => {
  return [];
};

export default scrapePortsmouthFC;
