// Stoke City official store (store.stokecityfc.com) — JonasSports platform.
// The store carries only leisurewear, gifts, and retro items — no current replica kits.
import { Product } from '../../shared/Product';

const scrapeStokeCity = async (): Promise<Product[]> => {
  return [];
};

export default scrapeStokeCity;
