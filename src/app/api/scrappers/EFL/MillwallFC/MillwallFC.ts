// Millwall FC official store (shop.millwallfc.co.uk) — Akamai bot protection.
// All programmatic requests are blocked with HTTP 403 (Akamai EdgeSuite).
// A browser-automation solution would be needed to bypass this.
import { Product } from '../../shared/Product';

const scrapeMillwallFC = async (): Promise<Product[]> => {
  return [];
};

export default scrapeMillwallFC;
