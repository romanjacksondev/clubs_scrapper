// Blackburn Rovers official store (www.rovers.co.uk/store) — custom GC platform.
// All product listings are dynamically rendered via client-side JavaScript.
// No accessible REST/GraphQL API found; a browser-automation solution is needed.
import { Product } from '../../shared/Product';

const scrapeBlackburnRovers = async (): Promise<Product[]> => {
  return [];
};

export default scrapeBlackburnRovers;
