// Leicester City official store (shop.lcfc.com) — Magento with Hyvä/Vue frontend.
// All product listings are dynamically rendered via client-side JavaScript.
// Server-side HTML contains no product data; a browser-automation solution is needed.
import { Product } from '../../shared/Product';

const scrapeLeicesterCity = async (): Promise<Product[]> => {
  return [];
};

export default scrapeLeicesterCity;
