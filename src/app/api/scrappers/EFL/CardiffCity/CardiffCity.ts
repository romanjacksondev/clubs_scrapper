// Cardiff City official store (www.cardiffcityfcstore.com) — JonasSports platform (new template).
// Product listings are fully JS-rendered; the category ID is not embedded in static HTML.
// A browser-automation solution is needed to extract products.
import { Product } from '../../shared/Product';

const scrapeCardiffCity = async (): Promise<Product[]> => {
  return [];
};

export default scrapeCardiffCity;
