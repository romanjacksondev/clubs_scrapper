// Coventry City official store (shop.ccfc.co.uk) — connection times out consistently.
// The store may have geo-restrictions or anti-scraping measures at the network level.
import { Product } from '../../shared/Product';

const scrapeCoventryCity = async (): Promise<Product[]> => {
  return [];
};

export default scrapeCoventryCity;
