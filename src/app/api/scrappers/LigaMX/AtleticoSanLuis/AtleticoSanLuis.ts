// Atlético de San Luis official store — atleticosanluis.com.
// Store is inaccessible to programmatic requests (server error / WAF).
// TODO: revisit if the store becomes accessible.

import { Product } from '../../shared/Product';

export default async function scrapeAtleticoSanLuis(): Promise<Product[]> {
  return [];
}
