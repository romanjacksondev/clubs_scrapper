// Querétaro FC (Gallos Blancos) official store — gallos.mx.
// No accessible public store API found.
// TODO: revisit if a public store API becomes available.

import { Product } from '../../shared/Product';

export default async function scrapeQueretaroFC(): Promise<Product[]> {
  return [];
}
