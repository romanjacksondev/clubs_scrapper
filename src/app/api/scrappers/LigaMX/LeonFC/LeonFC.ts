// León FC official store — clubleon.com.
// Store returns HTTP 403 to programmatic requests.
// TODO: revisit if the store becomes accessible.

import { Product } from '../../shared/Product';

export default async function scrapeLeonFC(): Promise<Product[]> {
  return [];
}
