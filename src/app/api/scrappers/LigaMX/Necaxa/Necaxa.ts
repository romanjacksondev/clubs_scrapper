// Necaxa official store — clubnecaxa.com.
// No accessible public store API found; the store frontend is not programmatically reachable.
// TODO: revisit if a public store API becomes available.

import { Product } from '../../shared/Product';

export default async function scrapeNecaxa(): Promise<Product[]> {
  return [];
}
