// CF Monterrey (Rayados) official store (tienda.rayados.com) — Cloudflare-protected.
// All programmatic requests receive a 403 / Cloudflare challenge page.
// TODO: revisit if the store becomes accessible.

import { Product } from '../../shared/Product';

export default async function scrapeCFMonterrey(): Promise<Product[]> {
  return [];
}
