// Club América official store (tiendaclubamerica.com.mx).
// The domain is currently unresolvable and the store is inaccessible to
// programmatic requests. Returning empty until the store is reachable.

import { Product } from '../../shared/Product';

const scrapeClubAmerica = async (): Promise<Product[]> => {
  return [];
};

export default scrapeClubAmerica;
