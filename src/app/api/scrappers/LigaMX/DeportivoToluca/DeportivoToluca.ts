// Deportivo Toluca FC official store — deportivotoluca.mx.
// Store is inaccessible to programmatic requests (no accessible store API found).
// TODO: revisit if a public store API becomes available.

import { Product } from '../../shared/Product';

export default async function scrapeDeportivoToluca(): Promise<Product[]> {
  return [];
}
