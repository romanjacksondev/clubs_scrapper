// Club Puebla official store — clubpuebla.com.
// Store is inaccessible to programmatic requests (HTTP 530 / Cloudflare).
// TODO: revisit if the store becomes accessible.

import { Product } from '../../shared/Product';

export default async function scrapeClubPuebla(): Promise<Product[]> {
  return [];
}
