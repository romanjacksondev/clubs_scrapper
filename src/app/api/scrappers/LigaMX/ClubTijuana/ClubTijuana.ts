// Club Tijuana (Xoloitzcuintles) official store — xoloitzcuintles.com.
// Store is inaccessible to programmatic requests (SSL certificate mismatch / WAF).
// TODO: revisit if the store becomes accessible.

import { Product } from '../../shared/Product';

export default async function scrapeClubTijuana(): Promise<Product[]> {
  return [];
}
