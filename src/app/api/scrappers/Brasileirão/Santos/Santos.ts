import { Product } from '../../shared/Product';

// Santos official store (loja.santosfc.com.br)
// TODO: Store unreachable from outside Brazil; platform not identified.
export default async function scrapeSantos(): Promise<Product[]> {
  return [];
}
