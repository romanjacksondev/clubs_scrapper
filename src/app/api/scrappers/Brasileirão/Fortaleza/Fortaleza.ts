import { Product } from '../../shared/Product';

// Fortaleza official store (loja.fortalezaec.com.br)
// TODO: Store unreachable from outside Brazil; platform not identified.
export default async function scrapeFortaleza(): Promise<Product[]> {
  return [];
}
