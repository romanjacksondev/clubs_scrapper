import { Product } from '../../shared/Product';

// Sport Recife official store (loja.sport.com.br)
// TODO: Store unreachable from outside Brazil; platform not identified.
export default async function scrapeSportRecife(): Promise<Product[]> {
  return [];
}
