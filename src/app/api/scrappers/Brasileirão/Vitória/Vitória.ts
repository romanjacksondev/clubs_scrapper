import { Product } from '../../shared/Product';

// Vitória official store (loja.ecvitoria.com.br)
// TODO: Store returned HTTP 530 (Cloudflare origin error); platform not identified.
export default async function scrapeVitoria(): Promise<Product[]> {
  return [];
}
