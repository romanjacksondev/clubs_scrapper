import { Product } from '../../shared/Product';

// Mirassol official store (loja.mirassolfc.com.br)
// TODO: Small club — no accessible online store found.
export default async function scrapeMirassol(): Promise<Product[]> {
  return [];
}
