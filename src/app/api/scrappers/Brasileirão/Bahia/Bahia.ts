import { Product } from '../../shared/Product';

// Bahia official store (loja.esporteclubebahia.com.br)
// TODO: Store unreachable from outside Brazil; platform not identified.
export default async function scrapeBahia(): Promise<Product[]> {
  return [];
}
