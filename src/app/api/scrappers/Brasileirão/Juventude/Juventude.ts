import { Product } from '../../shared/Product';

// Juventude official store (loja.ecjuventude.com.br)
// TODO: Store unreachable from outside Brazil; platform not identified.
export default async function scrapeJuventude(): Promise<Product[]> {
  return [];
}
