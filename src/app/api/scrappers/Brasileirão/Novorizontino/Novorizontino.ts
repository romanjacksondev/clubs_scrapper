import { Product } from '../../shared/Product';

// Novorizontino official store (loja.novorizontino.com.br)
// TODO: Small club — no accessible online store found.
export default async function scrapeNovorizontino(): Promise<Product[]> {
  return [];
}
