// San Martín de Tucumán official store: no accessible online store found.
// No public product API has been identified for this club.
// TODO: revisit if an official store becomes publicly accessible.

import { Product } from '../../shared/Product';

export default async function scrapeSanMartínTucumán(): Promise<Product[]> {
  return [];
}
