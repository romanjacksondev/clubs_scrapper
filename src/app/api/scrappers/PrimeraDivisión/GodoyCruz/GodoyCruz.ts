// Godoy Cruz official store: no accessible online store found.
// Domain candidates (tiendagodoycruz.com.ar, godoycruztomba.com) are not resolvable.
// TODO: revisit if an official store becomes publicly accessible.

import { Product } from '../../shared/Product';

export default async function scrapeGodoyCruz(): Promise<Product[]> {
  return [];
}
