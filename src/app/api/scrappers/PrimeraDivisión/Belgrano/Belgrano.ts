// Belgrano official store: no accessible online store found.
// The official site (cabelgrano.com.ar) has SSL issues and does not expose a product API.
// TODO: revisit if an official store becomes publicly accessible.

import { Product } from '../../shared/Product';

export default async function scrapeBelgrano(): Promise<Product[]> {
  return [];
}
