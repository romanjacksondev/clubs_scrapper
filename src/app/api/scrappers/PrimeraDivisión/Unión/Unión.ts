// Unión (Santa Fe) official store: tiendaunion.com.ar
// The store uses a custom platform — not Shopify, TiendaNube, or WooCommerce.
// No public product API has been identified.
// TODO: revisit if the platform exposes a public product endpoint.

import { Product } from '../../shared/Product';

export default async function scrapeUnión(): Promise<Product[]> {
  return [];
}
