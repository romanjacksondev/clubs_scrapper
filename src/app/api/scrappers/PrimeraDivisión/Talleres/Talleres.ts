// Talleres (Córdoba) official store: no accessible online store found.
// Domain candidates (tiendatalleres.com.ar, talleres.net) are not resolvable.
// TODO: revisit if an official store becomes publicly accessible.

import { Product } from '../../shared/Product';

export default async function scrapeTalleres(): Promise<Product[]> {
  return [];
}
