// Instituto (Córdoba) official store: tiendainstituto.com.ar
// The store uses the TiendaNube platform (mitiendanube.com CDN), which requires
// an authenticated API token for programmatic product access.
// TODO: revisit if a public API or token becomes available.

import { Product } from '../../shared/Product';

export default async function scrapeInstituto(): Promise<Product[]> {
  return [];
}
