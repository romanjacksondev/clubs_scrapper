// Patronato (Paraná) official store: no accessible online store found.
// Domain candidates (patronato.com.ar, tiendapatronato.com.ar) are not resolvable.
// TODO: revisit if an official store becomes publicly accessible.

import { Product } from '../../shared/Product';

export default async function scrapePatronato(): Promise<Product[]> {
  return [];
}
