// Banfield official store: no accessible online store found.
// Domain candidates (tiendabanfield.com.ar, clubbanfield.com.ar) are not resolvable.
// TODO: revisit if an official store becomes publicly accessible.

import { Product } from '../../shared/Product';

export default async function scrapeBanfield(): Promise<Product[]> {
  return [];
}
