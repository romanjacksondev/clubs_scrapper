// Atlético Tucumán official store: no accessible online store found.
// The official site (atleticotucuman.com) does not expose a public product API.
// TODO: revisit if an official store becomes publicly accessible.

import { Product } from '../../shared/Product';

export default async function scrapeAtléticoTucumán(): Promise<Product[]> {
  return [];
}
