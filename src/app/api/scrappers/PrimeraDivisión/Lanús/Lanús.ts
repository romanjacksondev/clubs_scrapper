// Lanús official store: tiendagranate.clublanus.com
// The store does not respond to Shopify or other known product API endpoints.
// Platform is unidentified; programmatic access is not available.
// TODO: revisit if the platform is identified and a public API is exposed.

import { Product } from '../../shared/Product';

export default async function scrapeLanús(): Promise<Product[]> {
  return [];
}
