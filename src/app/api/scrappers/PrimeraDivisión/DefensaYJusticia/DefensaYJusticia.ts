// Defensa y Justicia official store: no accessible online store found.
// All known domain candidates are not resolvable from outside Argentina.
// TODO: revisit if an official store becomes publicly accessible.

import { Product } from '../../shared/Product';

export default async function scrapeDefensaYJusticia(): Promise<Product[]> {
  return [];
}
