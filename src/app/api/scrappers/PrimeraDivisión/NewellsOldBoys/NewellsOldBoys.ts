// Newell's Old Boys official store: no accessible online store found.
// Domain candidates (tiendanob.com.ar, tiendanewells.com.ar) are not resolvable.
// TODO: revisit if an official store becomes publicly accessible.

import { Product } from '../../shared/Product';

export default async function scrapeNewellsOldBoys(): Promise<Product[]> {
  return [];
}
