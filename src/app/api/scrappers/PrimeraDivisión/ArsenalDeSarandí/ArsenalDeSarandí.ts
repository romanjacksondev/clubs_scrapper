// Arsenal de Sarandí official store: no accessible online store found.
// All known domain candidates (arsenal-fc.com.ar, tienda.arsenal-fc.com.ar) are
// not resolvable from outside Argentina.
// TODO: revisit if an official store becomes publicly accessible.

import { Product } from '../../shared/Product';

export default async function scrapeArsenalDeSarandí(): Promise<Product[]> {
  return [];
}
