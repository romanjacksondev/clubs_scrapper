// Platense official store: no accessible online store found.
// The official site (clubplatense.net) does not link to an online store.
// TODO: revisit if an official store becomes publicly accessible.

import { Product } from '../../shared/Product';

export default async function scrapePlatense(): Promise<Product[]> {
  return [];
}
