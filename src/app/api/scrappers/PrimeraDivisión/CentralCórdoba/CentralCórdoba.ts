// Central Córdoba (Santiago del Estero) official store: no accessible online store found.
// The official site (centralcordoba.com.ar) does not expose a product API.
// TODO: revisit if an official store becomes publicly accessible.

import { Product } from '../../shared/Product';

export default async function scrapeCentralCórdoba(): Promise<Product[]> {
  return [];
}
