// Sarmiento (Junín) official store: no accessible online store found.
// Domain candidates (tiendasarmiento.com.ar, clubsarmiento.com) return 404.
// TODO: revisit if an official store becomes publicly accessible.

import { Product } from '../../shared/Product';

export default async function scrapeSarmiento(): Promise<Product[]> {
  return [];
}
