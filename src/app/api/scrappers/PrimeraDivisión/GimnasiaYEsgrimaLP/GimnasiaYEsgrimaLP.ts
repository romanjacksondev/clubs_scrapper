// Gimnasia y Esgrima La Plata official store: no accessible online store found.
// Domain candidates (gimnasiaylp.com.ar, tiendaginmasia.com.ar) are not resolvable.
// TODO: revisit if an official store becomes publicly accessible.

import { Product } from '../../shared/Product';

export default async function scrapeGimnasiaYEsgrimaLP(): Promise<Product[]> {
  return [];
}
