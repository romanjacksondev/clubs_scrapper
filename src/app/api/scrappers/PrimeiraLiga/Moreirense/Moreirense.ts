// Moreirense FC official store: no accessible online store found.
// The official site (moreirensefc.pt) does not link to an online store.
// TODO: revisit if an official store becomes publicly accessible.

import { Product } from '../../shared/Product';

export default async function scrapeMoreirense(): Promise<Product[]> {
  return [];
}
