// AVS Futebol SAD official store: no accessible online store found.
// This club (formerly Vitória de Setúbal) has no identifiable public product API.
// TODO: revisit if an official store becomes publicly accessible.

import { Product } from '../../shared/Product';

export default async function scrapeAVS(): Promise<Product[]> {
  return [];
}
