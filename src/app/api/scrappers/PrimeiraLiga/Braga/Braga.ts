// SC Braga official store: store.scbraga.pt
// The store is a WordPress-based site (not Shopify or WooCommerce with public API).
// Programmatic access to the product catalogue is not available.
// TODO: revisit if a public product API or Shopify migration occurs.

import { Product } from '../../shared/Product';

export default async function scrapeBraga(): Promise<Product[]> {
  return [];
}
