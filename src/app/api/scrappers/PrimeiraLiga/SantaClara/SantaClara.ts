// CD Santa Clara official store: no accessible online store found.
// The official site (cdsantaclara.pt) does not expose a public product API.
// TODO: revisit if an official store becomes publicly accessible.

import { Product } from '../../shared/Product';

export default async function scrapeSantaClara(): Promise<Product[]> {
  return [];
}
