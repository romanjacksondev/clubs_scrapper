// Estrela da Amadora official store: no accessible online store found.
// Domain candidates (estreladaamadora.pt, estreladaamadora.com) are not resolvable.
// TODO: revisit if an official store becomes publicly accessible.

import { Product } from '../../shared/Product';

export default async function scrapeEstrelaAmadora(): Promise<Product[]> {
  return [];
}
