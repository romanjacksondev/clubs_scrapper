// SC Farense official store: no accessible online store found.
// Domain candidates (scfarense.com, scfarense.pt) are not resolvable from outside Portugal.
// TODO: revisit if an official store becomes publicly accessible.

import { Product } from '../../shared/Product';

export default async function scrapeFarense(): Promise<Product[]> {
  return [];
}
