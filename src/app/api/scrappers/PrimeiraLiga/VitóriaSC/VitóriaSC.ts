// Vitória SC (Guimarães) official store: loja.vitoriasc.pt
// The store redirects and does not expose a Shopify or other public product API.
// TODO: revisit if a public product endpoint becomes available.

import { Product } from '../../shared/Product';

export default async function scrapeVitóriaSC(): Promise<Product[]> {
  return [];
}
