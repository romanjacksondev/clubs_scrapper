// FC Famalicão official store: no accessible online store found.
// The official site (fcfamalicao.com) redirects to a portal page with no product API.
// TODO: revisit if an official store becomes publicly accessible.

import { Product } from '../../shared/Product';

export default async function scrapeFamalicão(): Promise<Product[]> {
  return [];
}
