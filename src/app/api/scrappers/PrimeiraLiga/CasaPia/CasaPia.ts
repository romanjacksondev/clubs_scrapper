// Casa Pia AC official store: casapiaac.pt/loja (also listed on desportiva.pt)
// The store is hosted on a third-party platform (desportiva.pt) that does not
// expose a public product API.
// TODO: revisit if a Shopify migration or public API becomes available.

import { Product } from '../../shared/Product';

export default async function scrapeCasaPia(): Promise<Product[]> {
  return [];
}
