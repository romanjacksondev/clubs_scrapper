// Gil Vicente FC official store: no accessible online store found.
// The official domain (gilvicente.pt) does not resolve from outside Portugal.
// TODO: revisit if an official store becomes publicly accessible.

import { Product } from '../../shared/Product';

export default async function scrapeGilVicente(): Promise<Product[]> {
  return [];
}
